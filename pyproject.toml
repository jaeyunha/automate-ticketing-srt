[project]
name = "automate-ticketing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "desktop-notifier>=6.2.0",
    "macos-notifications>=0.2.1",
]
